import { CustomersPage } from "@/components/customers/CustomersPage";

export const metadata = { title: "Customers — Interior Home Services BC" };

export default function Page() {
  return <CustomersPage />;
}
